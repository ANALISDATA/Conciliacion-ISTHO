# Librerías que necesita la app. El servidor las instala solo al desplegar.
#
# IMPORTANTE: las versiones van FIJAS a propósito.
# El 2 de agosto de 2026 la app dejó de arrancar en el servidor sin que se hubiera
# tocado una sola línea de código: Starlette (librería interna de Streamlit) publicó
# una versión nueva que le agregó un argumento obligatorio a GZipResponder, y el
# Streamlit instalado no lo enviaba:
#
#   TypeError: GZipResponder.__init__() missing 1 required keyword-only
#              argument: 'thread_minimum_size'
#
# Con versiones abiertas (>=) cada reinicio del servidor instala lo más reciente, así
# que una actualización ajena puede tumbar la app en cualquier momento. Fijándolas,
# cada despliegue instala exactamente lo mismo que ya se probó.
# Para actualizar: subir la versión aquí a propósito y comprobar que sigue funcionando.

streamlit==1.60.0
starlette==1.3.1     # 1.3.1 funciona; versiones posteriores rompen Streamlit 1.60

# Estas admiten rango: el código funciona igual con pandas 2 y 3 (la conversión de
# celdas vacías se hace valor por valor justamente por eso). El tope evita que una
# versión mayor futura entre sin avisar.
pandas>=2.2,<4
openpyxl>=3.1,<4     # leer .xlsx
XlsxWriter>=3.2,<4   # generar los Excel con membrete
